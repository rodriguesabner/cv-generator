import styled from "styled-components";

const Layout = styled.div`
    display: flex;
    flex-direction: row;
    align-items: center;

    .move__icon {
        cursor: move;
    }
`;

export {
    Layout
}